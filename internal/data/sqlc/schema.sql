CREATE TABLE spys (
    id BIGSERIAL PRIMARY KEY ,
    ip VARCHAR NOT NULL ,
    kind smallint NOT NULL ,
    created_at TIMESTAMP NOT NULL DEFAULT TIMEZONE('UTC-8'::TEXT, NOW()::TIMESTAMP(0) WITHOUT TIME ZONE)
);

CREATE TABLE users (
    id BIGSERIAL PRIMARY KEY ,
    name VARCHAR NOT NULL
);
